# Single source of truth for RUNTIME dependencies (NFR-M1, INV-5).
# Installed by both the host environment and any container. Dev and tooling
# pins live in requirements-dev.txt; do not add tools here.
duckdb==1.5.4
pydantic==2.10.6
pydantic-settings==2.7.1
streamlit==1.59.2
# Phase 2 AWS store adapters (ADR-0003). boto3 for S3/DynamoDB; pyiceberg with
# the Glue catalog and PyArrow file IO extras for the aggregate-of-record MERGE;
# pyarrow for the Iceberg data plane. All imported lazily under adapters/ only.
boto3==1.43.51
pyiceberg[glue,pyarrow]==0.11.1
pyarrow==24.0.0
# Kafka client for the live-broker path (ADR-0002). Imported lazily inside the
# transport adapter only, so offline test collection never loads it; the
# container stack and the P2-T3 broker smoke are the first users. The wheel
# bundles librdkafka (aarch64 and x86_64), so the slim image needs no build step.
confluent-kafka==2.11.1
